


  <% provide(:title, 'Sign up') %>
    <h1>注册账号</h1>
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
<%= form_for(@user) do |f| %>
<%= render 'fields', f: f %>
<%= f.submit "创建我的账户", class: "btn btn-primary" %>
<% end %> </div>
</div>